# Example only. Keep the real Preview value in Vercel Environment Variables.
# Point a staging-branch Preview deployment to the staging Render backend.
NEXT_PUBLIC_API_URL="https://your-staging-backend.onrender.com"
