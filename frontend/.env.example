# Server-side target used by the Next.js same-origin API/session proxy.
# The same value is also used by server-rendered session checks.
# Do not include a trailing slash.
NEXT_PUBLIC_API_URL="http://localhost:4000"
