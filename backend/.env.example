# Required PostgreSQL connection
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/shodhfund?schema=public"

# Required in production: use a unique random value with at least 32 characters.
JWT_SECRET="replace-with-a-random-secret-at-least-32-characters-long"

PORT=4000
CORS_ORIGIN="http://localhost:3000"

# Optional live bill extraction. GOOGLE_API_KEY is also accepted.
GEMINI_API_KEY=""
# GEMINI_MODEL="gemini-2.0-flash"

# Optional configuration indicators only. Current health reporting does not test
# storage connectivity and the expense workflow does not upload source bills.
R2_ENDPOINT=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET=""
